# Auralith Agent 内置功能测试

## 本轮边界

`Auralith Agent` 是套件级智能侧边栏，不是 DOCX Reader 插件。当前测试版只开放
第一个能力：对当前 DOCX 进行文字、图片与页面关系的多模态读取和问答。

- DOCX：显示多模态读取、覆盖报告、图文摘要、问答和证据定位。
- 电子表格、演示文稿、PDF、流程图：保留同一个 Agent 入口，显示该界面的能力
  尚未接入，并允许预先配置 Provider 和模型。
- Agent 资源由编辑器壳直接加载，不注册 GUID，也不会出现在“插件”页签中。

## 启动

双击桌面的 `Auralith_Editer Test` 快捷方式。测试程序会自动打开包含正文、图片
和图注的黄金样本文档。

也可以直接运行：

```powershell
& "$env:LOCALAPPDATA\Auralith_Editer\TestBuild\Auralith_Editer.Launcher.exe"
```

启动器只监控隔离测试目录中的编辑器进程，并把测试窗口标题保持为
`Auralith_Editer`；不会修改系统正式安装。

## 首次配置

1. 打开样例 DOCX。`Auralith Agent` 按钮停靠在编辑器头部右侧的按钮组中
   （无头部插槽的页面回退为右上角悬浮按钮）。
2. LLM 设置位于 **文件 → 高级设置 → Auralith Agent**：添加 Provider 并选择
   支持图片和结构化输出的模型；“模型能力与高级配置”折叠区提供手动声明与
   最小能力测试；“数据与下载权限”提供两项独立授权。
3. 侧边栏头部的模型胶囊会实时显示当前模型；点击它（或“打开模型设置”）会
   跳转到高级设置页。设置更改通过 storage 事件在两个界面间即时同步。
4. 阅读远程处理说明，并明确授权后开始读取。

模型能力测试与文档分析可能产生服务费用。未授权时，文档证据不会发送到远程
模型；OLE、宏和外部代码不会执行，外链图片不会自动下载。

侧边栏加载的是随应用构建、同源且受信任的 Auralith Agent 代码，以便使用
IndexedDB、Web Worker 和本地模型缓存；它不会加载第三方 UI。与编辑器之间的
消息桥仍只允许五个只读快照方法，并同时校验来源窗口、origin 和每次随机 channel。

## 建议测试

1. 确认插件列表中没有独立的 `DOCX Reader`，入口只显示为
   `Auralith Agent`，且停靠在头部按钮组内、不遮挡搜索或用户按钮。
2. 在 文件 → 高级设置 中切换模型，回到侧边栏确认模型胶囊与读取状态即时
   更新；在设置页撤销“远程文档证据”授权，确认进行中的模型请求被中止。
3. 等待状态变为“可提问”，检查正文、页面、对象和图片覆盖数量。
4. 提问：“这张图片展示了什么？”
5. 提问：“图片、图注和相邻段落之间是什么关系？”
6. 提问：“概括全文，并分别引用正文和图片证据。”
7. 点击回答中的证据，确认编辑器定位到对应段落、图片或页面。
8. 修改正文或移动图片，等待约一秒，确认旧答案被标为旧快照并重新读取。
9. 在读取或回答过程中取消，再执行重试。
10. 打开其他格式，确认仍有 `Auralith Agent` 入口，但不会误调用 DOCX 接口。

## 当前原生壳限制

此隔离测试版使用现有预编译桌面 EXE，因此本轮可直接测试的是各编辑器界面中的
内置 Agent。要让开始页也使用完全相同的原生标题栏入口，必须重编
`desktop-apps` 的 Qt 桌面壳；当前机器未安装该项目所需的 Qt/MSVC 构建链。
源代码集成点已定位在主窗口标题栏与标签容器，不能用插件注册来冒充这一层能力。

## 在 macOS 恢复完整源码

本项目使用标准 Git submodule 方案。三个包含 Auralith 修改的组件分别托管在
`Yecyi/desktop-sdk`、`Yecyi/web-apps` 和 `Yecyi/sdkjs`，根仓库固定其提交。

```bash
git clone https://github.com/Yecyi/Auralith_Editer.git
cd Auralith_Editer
bash tools/bootstrap-auralith-macos.sh
```

脚本会自动下载三个子模块、核对固定提交、配置官方 upstream，并切换到可继续
开发和推送的本地 `auralith/master` 分支。完整交接见 `MACOS_HANDOFF.md`。
