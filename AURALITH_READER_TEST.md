# Auralith_Editer DOCX 多模态阅读测试

## 启动

双击桌面的 `Auralith_Editer Test` 快捷方式。测试程序会自动打开包含正文、图片和图注的黄金样本文档。

也可以直接运行：

```powershell
& "$env:LOCALAPPDATA\Auralith_Editer\TestBuild\Auralith_Editer.Launcher.exe"
```

启动器只监控隔离测试目录中的编辑器进程，并把测试窗口标题保持为
`Auralith_Editer`；不会修改系统正式安装或其他编辑器窗口。

## 首次配置

1. 打开 `Auralith_Editer AI` 设置。
2. 选择 OpenAI、Anthropic、Gemini 或兼容服务，并选择支持图片和结构化输出的模型。
3. 点击模型能力测试，确认视觉、结构化输出和上下文窗口都通过。
4. 打开 `Auralith_Editer DOCX multimodal reader`。
5. 首次远程分析时阅读授权说明，再决定是否允许发送当前命中的证据。

## 建议测试

1. 等待状态变为“可提问”，检查正文、页面、对象和图片覆盖数量。
2. 提问：“这张图片展示了什么？”
3. 提问：“图片、图注和相邻段落之间是什么关系？”
4. 提问：“概括全文，并分别引用正文和图片证据。”
5. 点击回答中的证据，确认编辑器能定位到对应段落、图片或页面。
6. 修改正文或移动图片，等待约一秒，确认旧答案被标为旧快照，并自动重新读取。
7. 在读取或回答过程中点击取消，再执行重试。

模型调用可能产生服务费用。未授权时，文档证据不会发送到远程模型。
